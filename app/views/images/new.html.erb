<% if params[:product_id] || params[:post_id] %>

<%= render :partial => "form" %>

<% else %>

<h1>New image</h1>

<% form_for(@image, :html => {:multipart => true}) do |f| %>
  <%= f.error_messages %>
  <%= render :partial => "form", :locals => {:f =>f} %>
  <p>
    <%= f.submit "Create" %>
  </p>
  <%  %>
<% end %>

<%= link_to 'Back', images_path %>

<% end %>
